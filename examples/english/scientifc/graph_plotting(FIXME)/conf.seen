() -> {
    { name: "graph_plotting" }
}

py -> {
    py_path:"python3",       // read from env.seen
    pkg_man: "pip.seen",     // is the same as "./conf/py/pip.seen" , the dir is inferred from py-> 
    install: "local",
    deps: [
        "matplotlib",
        "mpl3d"
    ]
}